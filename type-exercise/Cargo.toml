[package]
name = "type-exercise"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[features]
# Compile the crate (and its supplied tests) against an opaque, unpinnable
# BindError/ExpressionError layout for the learner-compatibility fixture.
opaque-errors = []

[dependencies]
bitvec = "1"

[dev-dependencies]
criterion = "0.8.2"

[[bench]]
name = "expression"
harness = false
